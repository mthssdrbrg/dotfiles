[Unit]
Description=Mopidy music server
After=avahi-daemon.service
After=dbus.service
After=network-online.target
After=nss-lookup.target
After=sound.target

[Service]
ExecStart=/usr/bin/mopidy --config %h/.config/mopidy

[Install]
WantedBy=default.target
